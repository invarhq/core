## Core Functions for VioletBrick Packages

Edit your project's `composer.json` file to require `violetbrick/core`.

    "require": {
        "violetbrick/core": "dev-master"
    }

Next, update Composer from the Terminal:

    composer update
